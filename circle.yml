machine:
  hosts:
    rabbitmq: 127.0.0.1
  services:
    - rabbitmq-server
  node:
    version: v0.10.31
dependencies:
  pre:
    - npm login $NPM_USER $NPM_PASSWORD
general:
  artifacts:
    - coverage/*
    - coverage/**/*
    - log/*.log
    - npm-debug.log

