machine:
  hosts:
    rabbitmq: 127.0.0.1
  services:
    - rabbitmq-server
  node:
    version: iojs-v1.8
general:
  artifacts:
    - coverage/*
    - coverage/**/*
    - log/*.log
    - npm-debug.log

